FastAPI Skill

This skill provides templates and scripts for building FastAPI endpoints,
Pydantic models, and simple tests. Use `templates/` to store starter files
and `scripts/` for small helper scripts (e.g., runserver.sh).

Examples:
- templates/app_template.py
- templates/model_template.py
